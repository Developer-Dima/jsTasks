function doit(){
	for (var i = 9; i > 0; i--) {
		for (var a = 0; a < i; a++) {
			console.log("sdadas")
				button.insertAdjacentHTML("afterend", i);
			}
		button.insertAdjacentHTML("afterend", "<br />");
	}
	};